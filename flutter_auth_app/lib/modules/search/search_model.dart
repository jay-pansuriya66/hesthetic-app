// lib/models/search_model.dart

/// Model representing a single search result item.
/// For simplicity, we’ll just store a string title.
class SearchModel {
  final String title;

  SearchModel({required this.title});
}
